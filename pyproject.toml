[project]
name = "stream-interactive-workbench"
version = "0.1.0"
description = "Reusable live-stream interaction overlays and a GitHub Pages showcase"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/stream_interactive"]
